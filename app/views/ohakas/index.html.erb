<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, user-scalable=no, initial-scale=1, maximum-scale=1"/>
  </head>
  <body>
      <div id="top-ohaka">
        <h1>お墓トップです</h1>
        <%= link_to 'お墓を立てる', new_ohaka_path, class: "btn btn-default btn-block" %>
        <p>
          <h2>お墓検索</h2>
          <%= form_for :ohaka do |f| %>
          <%= f.label :name, "名前", class: "col-lg-2 control-label" %>
          <%= f.text_field :name, class: "form-control" %>
          <%= f.submit '検索', class: "btn btn-default btn-block" %>
          <% end %>

          <% @ohakas.each do |ohaka| %>
          <p>
            <%= ohaka.name %><br>
            <%= image_tag ohaka.image.thumb.url %>
            <%= link_to '編集', edit_ohaka_path(ohaka) %>
            <%= link_to '削除', ohaka_path(ohaka),
              method: :delete, data: { confirm: '消しますか？' } %>
          </p>
          <% end %>
        </p>
      </div>
  </body>
  </html>
